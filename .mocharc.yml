color: true
recursive: true
bail: true # to see which tests are failing, not just the first
require:
  - 'ts-node/register'
  - 'source-map-support/register'
extension:
  - ts
ui: 'bdd'
check-leaks: true
fullStackTrace: true
